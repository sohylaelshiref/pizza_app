name: pizza_repository
description: "the repo that handel pizzas."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # cupertino_icons: ^1.0.8
  # firebase_auth: ^6.0.2
  # cloud_firestore: ^6.0.1
  # firebase_storage: ^13.0.1
  # equatable: ^2.0.7
  # rxdart: ^0.28.0
  # path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true